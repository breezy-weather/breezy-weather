package wangdaye.com.geometricweather.Widget;
/*
import android.annotation.TargetApi;
import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Paint;
import android.os.Build;
import android.util.AttributeSet;
import android.view.View;
*/
/**
 * Show sky. (px = dp * (dpi / 160))
 * */
/*
public class SkyView extends View {
    // widget
    private Context context;
    private Paint paint;

    // data
    private final int UNIT_DP_SIZE = 120;
    private float unitPxSize;

    private final int FIRST_FLOOR_TIME = 300;
    private final int OTHERS_FLOOR_TIME = 400;
    private final int BACKGROUND_TIME = 1500;

    public SkyView(Context context) {
        super(context);
        this.initialize(context);
    }

    public SkyView(Context context, AttributeSet attrs) {
        super(context, attrs);
        this.initialize(context);
    }

    public SkyView(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
        this.initialize(context);
    }

    @TargetApi(Build.VERSION_CODES.LOLLIPOP)
    public SkyView(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
        this.initialize(context);
    }

    private void initialize(Context context) {
        this.context = context;
        this.paint = new Paint();

        int dpi = getResources().getDisplayMetrics().densityDpi;
        this.unitPxSize = UNIT_DP_SIZE * (dpi / 160);
    }

    @Override
    protected void onDraw(Canvas canvas) {

    }

    private void drawFirstFloor(Canvas canvas) {

    }

    private void drawSecondFloor(Canvas canvas) {

    }

    private void drawThirdFloor(Canvas canvas) {

    }

    private void drawForthFloor(Canvas canvas) {

    }
}
*/